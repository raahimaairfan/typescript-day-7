/* Think of something you could store in a TypeScript Object.
Write a program that creates Objects containing these items. */
var studentInfo = {
    name: "Raahima",
    rollNo: 123,
    CurrentlyEnrolled: true
};
console.log("Student name is ".concat(studentInfo.name, " having roll number ").concat(studentInfo.rollNo, " and currently enrolled status is ").concat(studentInfo.CurrentlyEnrolled));
